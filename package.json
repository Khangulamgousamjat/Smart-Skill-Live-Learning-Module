{
  "name": "skill-developer-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "install": "npm install --prefix frontend",
    "postinstall": "npm install --prefix backend"
  }
}
